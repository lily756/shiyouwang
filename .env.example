OPENAI_API_KEY=your_api_key
OPENAI_API_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=deepseek-v4-pro
# Seed 2.0 的深度思考开关；false 会在主文本模型请求中传 thinking.type=disabled
OPENAI_THINKING_ENABLED=false
# 可选：图片 / sticker 自动使用此全模态模型；未设置时继续使用 OPENAI_MODEL
OPENAI_VISION_MODEL=
# 可选：如果全模态模型来自另一家 OpenAI 兼容服务，再分别填写其 Key 和 Base URL
OPENAI_VISION_API_KEY=
OPENAI_VISION_API_BASE_URL=
# 某些视觉提供商只接受公网 HTTPS 图片 URL。true 会将 Telegram 下载 URL（含 Bot Token）发送给视觉提供商；存在凭据泄露风险，仅在接受该风险时开启。
VISION_USE_TELEGRAM_FILE_URL=false
# 视觉图片短期 URL 代理；远程视觉提供商需要能访问这个地址
VISION_ASSET_SERVER_HOST=0.0.0.0
VISION_ASSET_SERVER_PORT=3000
VISION_ASSET_PUBLIC_BASE_URL=http://160.16.146.27:3000
# 推荐：用于加密各用户保存的麦当劳 MCP Token。填写随机的 32+ 字符密钥；未填写时会回退使用 TG_BOT_TOKEN。
MCD_TOKEN_ENCRYPTION_KEY=replace_with_a_long_random_secret
TG_BOT_TOKEN=your_telegram_bot_token
TG_ADMIN_USER_IDS=123456789
# 图片服务：配置 SEEDREAM_API_KEY 时默认自动使用 seedream；也可显式填写 seedream 或 newapi
IMAGE_PROVIDER=seedream
SEEDREAM_API_BASE_URL=https://vvdance.yongmuai.com
SEEDREAM_API_KEY=your_seedream_api_key
# 可选：dola-seedream-5-0-pro-260628（Pro）或 seedream-5-0-lite-260128（Lite）
SEEDREAM_MODEL=dola-seedream-5-0-pro-260628
# Pro 支持 1K、2K；Lite 支持 2K、3K、4K；也可按对应模型限制填写宽x高。默认 2K。
SEEDREAM_IMAGE_SIZE=2K
# 独立的 Seedream 5.0 Lite 测试脚本使用；按文档以 Bearer Token 发送 Key，不要将真实值提交到 Git。
SEEDREAM_LITE_API_KEY=
SEEDREAM_LITE_API_SECRET=
# Seedance 视频服务：Bearer Token 使用 VVDance 开发者 API Key；默认 Seedance 2.0 Mini、480p
SEEDANCE_API_BASE_URL=https://vvdance.ai
SEEDANCE_API_TOKEN=your_vvdance_api_key
SEEDANCE_VIDEO_MODEL=dreamina-seedance-2-0-mini-260615
SEEDANCE_VIDEO_RESOLUTION=480p
# 可选：默认横屏、智能时长并生成音频；时长可为 -1（智能）或 4～15
SEEDANCE_VIDEO_RATIO=16:9
SEEDANCE_VIDEO_DURATION=-1
SEEDANCE_VIDEO_GENERATE_AUDIO=true
# 以下为旧 NewAPI 图片路径的兼容配置；仅 IMAGE_PROVIDER=newapi 时使用
NEWAPI_BASE_URL=https://your-newapi-host/v1
NEWAPI_API_KEY=your_newapi_key
NEWAPI_IMAGE_MODEL=gemini-3.1-flash-image
# T2I 默认输出约 1080p 的竖图；可按模型能力调整
NEWAPI_IMAGE_SIZE=1080x1920
# I2I 使用 POST /v1/images/edits 的 multipart/form-data 格式
NEWAPI_IMAGE_EDIT_MODEL=your_newapi_image_edit_model
NEWAPI_IMAGE_EDIT_SIZE=1024x1024
# 可选：自托管 SearXNG 的地址；未配置时使用 DuckDuckGo
SEARXNG_BASE_URL=
